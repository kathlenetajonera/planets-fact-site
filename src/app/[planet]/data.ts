import { OverviewType } from './types';

export const overviewLabels: OverviewType = {
    rotation: 'Rotation Time',
    revolution: 'Revolution Time',
    radius: 'Radius',
    temperature: 'Average Temp.',
};

export const overviewKeys = Object.keys(overviewLabels);
